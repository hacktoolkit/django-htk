# Utils

## Functions
- **`compute_cpq_code`** (utils/crypto.py) - Computes the encoded id for a CPQ object (Quote or Invoice)
- **`resolve_cpq_code`** (utils/crypto.py) - Returns the CPQ object (Quote or Invoice) for this `cpq_code`
