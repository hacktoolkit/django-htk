# Customers

## Classes
- **`BaseCustomer`** (customers/models.py) - Base model for a Customer in the `htk.apps.cpq` app
- **`BaseOrganizationCustomer`** (customers/models.py) - Base model for an OrganizationCustomer in the `htk.apps.cpq` app

## Components
**Models** (`models.py`), **Forms** (`forms.py`)
