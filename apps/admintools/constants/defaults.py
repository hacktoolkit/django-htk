# URL for when developing inside `django-htk` for updating React APP
# Working directly in `django-htk` repository is not suggested (only with cloning `hacktoolkit/django-htk`)
# It should be developed inside a project.
# HTK_ADMINTOOLS_VITE_URL = 'https://localhost:3000'
HTK_ADMINTOOLS_VITE_URL = 'https://docker.dev.goodpeople.ai:3000'
