YUI().use(
    'node',
    'event',
function (Y) {
    // --------------------------------------------------
    // YUI "Local" Globals

    // CSS selectors

    // Nodes
    var main = Y.one('#main');

    // App variables

    // End YUI "Local" Globals
    // --------------------------------------------------

    // Custom App Functions


    // App Initializers
    function initEventHandlers() {
    }

    function init() {
    }
    initEventHandlers();
    init();
});
