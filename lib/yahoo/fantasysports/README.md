Yahoo Fantasy Sports API Django/Python Wrapper
==============================================

To test, add this entry into the root URLs conf, and visit that resource:

   `url(r'^lib/yahoo_fantasy/', include('htk.lib.yahoo.fantasysports.urls')),`
